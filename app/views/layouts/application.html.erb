<!DOCTYPE html>
<html>
  <head>
    <title>AppengineRails3</title>
    <%= stylesheet_link_tag :all %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
  </head>
  <body>

    <p class="login">
    <% if logged_in? -%>
      Hey, <%= current_user.nickname %>! <%= "(oh, and it looks like you're an admin too!)" if admin? %>. You can also <%= link_to 'logout', logout_url %>.
    <% else -%>
      Oh look, you can <%= link_to 'login', login_url %>!
    <% end -%>
    </p>

    <%= yield %>

  </body>
</html>
